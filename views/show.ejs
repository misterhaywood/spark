<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Notes</title>
  </head>
  <body>
    <h1>note</h1>
    <%= notes.name %> last updated by <br><br> <%= notes.content %>.
    
    <% for (let i=0; i< notes.length; i++){ %>
    <li>
      <%=notes[i].name%>
    </li>
    <% } %>
    <br><br>
    <form action="/notes/<%= notes.id %>?_method=DELETE" method="POST">
      <input type="submit" value="TRASH" />
    </form>
    <br />

    <a href="/notes">ALL NOTES</a>
  </body>
</html>