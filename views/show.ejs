<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>note App</title>
  </head>
  <body>
    <h1>note Show Page</h1>
    The <%= notes.name %> is <%= notes.content %>.
    
    <h3>It is available in xxx:</h3>
    <% for (let i=0; i< notes.length; i++){ %>
    <li>
      <%=notes[i].name%>
    </li>
    <br />
    <% } %>
    <form action="/notes/<%= notes.id %>?_method=DELETE" method="POST">
      <input type="submit" value="DELETE <%= notes.name %>" />
    </form>
  </body>
</html>